# Data Service Platform Web

Management UI for data service datasets, API definitions, app authorization, and runtime smoke checks.

Local defaults:

- Port: `5179`
- Admin API proxy: `http://localhost:8087`
- OpenAPI proxy: `http://localhost:8086`

Run:

```bash
npm install
npm run dev
```
